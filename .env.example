DISCORD_TOKEN=your_bot_token_from_the_dev_portal
CLIENT_ID=your_application_id
# Optional: set this while developing for instant command updates in one server.
# Remove/leave blank for production so commands register globally.
GUILD_ID=your_test_server_id
